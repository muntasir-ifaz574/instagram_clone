import 'package:flutter/material.dart';

class MobileScreenLayouy extends StatelessWidget {
  const MobileScreenLayouy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Mobile')),
    );
  }
}
